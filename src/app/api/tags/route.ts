import { NextRequest, NextResponse } from 'next/server'; import { handleApiError } from '@/server/errors/application-error'; import { requireSeeker } from '@/server/middleware/seeker-auth'; import { workspaceService } from '@/server/services/workspace-service'; import { tagInputSchema } from '@/server/validators/workspace-validator';
export async function GET() { try { const user = await requireSeeker(); return NextResponse.json({ success: true, tags: await workspaceService.listTags(user.id) }); } catch (error) { return handleApiError(error, 'Failed to list tags'); } }
export async function POST(request: NextRequest) { try { const user = await requireSeeker(); const tag = await workspaceService.createTag(user.id, tagInputSchema.parse(await request.json())); return NextResponse.json({ success: true, tag }, { status: 201 }); } catch (error) { return handleApiError(error, 'Failed to create tag'); } }
