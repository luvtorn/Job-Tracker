import { NextRequest, NextResponse } from 'next/server'; import { handleApiError } from '@/server/errors/application-error'; import { requireSeeker } from '@/server/middleware/seeker-auth'; import { workspaceService } from '@/server/services/workspace-service'; import { noteInputSchema } from '@/server/validators/workspace-validator';
export async function GET() { try { const user = await requireSeeker(); return NextResponse.json({ success: true, notes: await workspaceService.listNotes(user.id) }); } catch (error) { return handleApiError(error, 'Failed to list notes'); } }
export async function POST(request: NextRequest) { try { const user = await requireSeeker(); const note = await workspaceService.createNote(user.id, noteInputSchema.parse(await request.json())); return NextResponse.json({ success: true, note }, { status: 201 }); } catch (error) { return handleApiError(error, 'Failed to create note'); } }
